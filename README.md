# clean_linux 🧹

A simple and safe Linux cleanup utility.

## Features
- Cleans apt cache
- Removes unused packages
- Deletes temp files
- Clears user cache and trash
- Cleans old system logs

## Install
```bash
git clone https://github.com/yourname/clean_linux
cd clean_linux
chmod +x install.sh
sudo ./install.sh
